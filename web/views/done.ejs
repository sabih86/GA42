<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Setup Complete - LLMReport</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary: #3B82F6;
      --success: #059669;
      --gray-50: #F8FAFC;
      --gray-100: #F1F5F9;
      --gray-600: #475569;
      --gray-700: #334155;
      --gray-900: #0F172A;
      --white: #FFFFFF;
      --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      --radius-lg: 12px;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, var(--gray-50) 0%, var(--gray-100) 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .container {
      background: var(--white);
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-lg);
      padding: 3rem;
      width: 100%;
      max-width: 600px;
      text-align: center;
    }

    .success-icon {
      width: 4rem;
      height: 4rem;
      background: var(--success);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 2rem;
      color: var(--white);
      font-size: 2rem;
    }

    .header h1 {
      font-size: 2rem;
      font-weight: 700;
      color: var(--gray-900);
      margin-bottom: 1rem;
    }

    .header p {
      color: var(--gray-600);
      font-size: 1.125rem;
      margin-bottom: 2rem;
    }

    .file-path {
      background: var(--gray-50);
      border: 1px solid var(--gray-100);
      border-radius: var(--radius-lg);
      padding: 1rem;
      font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
      font-size: 0.875rem;
      color: var(--gray-700);
      margin-bottom: 2rem;
      word-break: break-all;
    }

    .command-section {
      background: var(--gray-900);
      color: var(--white);
      border-radius: var(--radius-lg);
      padding: 1.5rem;
      margin-bottom: 2rem;
    }

    .command-label {
      font-size: 0.875rem;
      color: var(--gray-600);
      margin-bottom: 0.5rem;
    }

    .command {
      font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
      font-size: 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .command-prompt {
      color: var(--success);
    }

    .next-steps {
      text-align: left;
      background: var(--gray-50);
      border-radius: var(--radius-lg);
      padding: 1.5rem;
    }

    .next-steps h3 {
      font-size: 1.125rem;
      font-weight: 700;
      color: var(--gray-900);
      margin-bottom: 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .steps-list {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .step-item {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
    }

    .step-number {
      background: var(--primary);
      color: var(--white);
      width: 1.5rem;
      height: 1.5rem;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.75rem;
      font-weight: 700;
      flex-shrink: 0;
      margin-top: 0.125rem;
    }

    .step-text {
      color: var(--gray-700);
      line-height: 1.5;
    }

    .step-code {
      background: var(--gray-900);
      color: var(--white);
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
      font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
      font-size: 0.875rem;
    }

    @media (max-width: 640px) {
      body {
        padding: 1rem;
      }

      .container {
        padding: 2rem;
      }

      .header h1 {
        font-size: 1.5rem;
      }

      .command {
        font-size: 0.875rem;
        flex-direction: column;
        align-items: flex-start;
        gap: 0.25rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="success-icon">
      <i class="fas fa-check"></i>
    </div>
    
    <div class="header">
      <h1>Setup Complete!</h1>
      <p>Your LLMReport input file has been successfully created</p>
    </div>

    <div class="file-path">
      <%= filePath %>
    </div>

    <div class="command-section">
      <div class="command-label">Run this command to start the analysis:</div>
      <div class="command">
        <span class="command-prompt">$</span>
        <span>node llmreportv7.js</span>
      </div>
    </div>

    <div class="next-steps">
      <h3>
        <i class="fas fa-list-check"></i>
        Next Steps
      </h3>
      <ol class="steps-list">
        <li class="step-item">
          <div class="step-number">1</div>
          <div class="step-text">
            Navigate to your project directory in the terminal
          </div>
        </li>
        <li class="step-item">
          <div class="step-number">2</div>
          <div class="step-text">
            Run <span class="step-code">node llmreportv7.js</span> to start the LLM analysis
          </div>
        </li>
        <li class="step-item">
          <div class="step-number">3</div>
          <div class="step-text">
            Wait for the analysis to complete (this may take several minutes)
          </div>
        </li>
        <li class="step-item">
          <div class="step-number">4</div>
          <div class="step-text">
            Return to the dashboard to view your results and insights
          </div>
        </li>
      </ol>
    </div>
  </div>
</body>
</html>
